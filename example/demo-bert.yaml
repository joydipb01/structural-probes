model:
  hidden_dim: 768 # hidden dim
  model_type: BERT-disk # BERT-disk, ELMo-disk, 
  use_disk: False
  model_layer: 2 # BERT-base: {1,...,12}; ELMo: {1,2,3}
probe:
  task_name: demo
  maximum_rank: 32
  psd_parameters: True
  depth_params_path: example/data/bertbase-depth-probe.params
  distance_params_path: example/data/bertbase-distance-probe.params
reporting:
  root: example/results

